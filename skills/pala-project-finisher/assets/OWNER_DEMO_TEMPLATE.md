# Owner Demo

- Güncelleme: `<tarih ve saat>`
- Sürüm/commit: `<kimlik veya not run>`
- Durum: `<gösterilebilir | kısmen gösterilebilir | blocked>`

## Şu anda kullanılabilen

`<Kullanıcının görebildiği tamamlanmış sonucu kısa anlat.>`

## Açma ve deneme

- URL/komut: `<yerel URL veya başlatma komutu>`
- Erişim: `<secretsız kurulum/giriş yöntemi>`

1. `<güvenli deneme adımı>`
2. `<beklenen görünür sonuç>`

## Görsel kanıt

- `<repo-relative ekran görüntüsü bağlantısı veya not run + nedeni>`

## Doğrulama

- Runtime: `<passed | failed | blocked | not run>` — `<kanıt>`
- Tarayıcı: `<passed | failed | blocked | not run>` — `<kanıt>`
- İlgili kalite kapısı: `<durum>` — `<komut/özet>`

## Bilinen sınır ve sonraki görünür sonuç

- Sınır: `<dürüst mevcut sınır>`
- Sıradaki: `<tam olarak bir görünür sonuç>`
